# 今回の調整

今回の変更では、既定感度を0.3以下へ下げ、曲名とアーティスト名の初期値を空欄にする。完了後に型チェック、本番ビルド、プレビューを確認する。

- [ ] ビジュアライザーのデフォルト感度・振幅を大幅に下げ、穏やかで滑らかな動きにする
- [ ] キャンバス内の曲名・アーティスト名などのオーバーレイテキストを削除する
- [ ] 型チェック・本番ビルド・プレビュー表示を確認する

今回の変更では、入力時のみ中央下へテキストを表示し、visibilitychange・beforeunload・アンマウント時に音声を停止する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、横線／二重円リングの切り替え、実音声連動の二重リングと放射状スペクトラム、既存色連動、ネオングローを追加する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、リング反応を大きくし、内側を時間変化するレインボー、外側を白へ固定する。書き出し中の推定残り時間を表示し、30fps・固定間隔描画・不要な高負荷処理削減で高速化する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、二重リングの直径を固定し、内外の円周から白い放射状バーを高密度に伸ばす。FFT反応を強化し、既定感度を高め、内側レインボー・外側白・バー白グローを維持する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、放射状バーを削除し、固定半径の内外リングをFFTで波打たせる。内側は時間変化レインボー、外側は白、双方にグローを付ける。iPhone SafariではMediaRecorder形式検出と共有／別タブ保存フォールバックを用意する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、二重リングのストロークを削除し、内側へ向かうレインボー面と外側へ向かう白い面をFFTで変形させる。iPhone保存はWeb Share APIを廃止し、Blobをdata URLへ変換して新規タブで開く。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、再生中のリング変形量を大幅に抑え、FFT平滑化を弱めてアタックへの追従を鋭くする。Web Share APIの全コードを削除し、iOSはObject URLを新しいタブで開く方式に統一する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、塗りつぶし面を削除し、細い二重リングと内外方向へ伸びる小さな針状スパイクへ刷新する。内側はレインボー、外側は白、双方にグローを付ける。window.openを削除し、書き出し後は同一ページ内videoプレビューと長押し保存案内を表示し、PCではダウンロードリンクも表示する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、現行二重リングを削除し、内側の太い白コア＋高速レインボー＋ビートパルス＋円周リプル、外側の1px白ローポリ波形＋ドット＋放射コームを加算合成で再実装する。書き出し後はiPhone向けダウンロードボタン、data URLクリック、videoプレビューと画面録画案内を用意する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、FFTサンプルを末尾・先頭でラップして閉ループ補間し、12時位置の段差を解消する。内側は半径変調±2〜3%と微細高周波リプルを分離し、白い芯＋広い彩度グローへ変更。外側半径は内側の約1.95倍にし、トゲは周波数振幅に応じて長さ・密度を変える。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、内側リングをR×0.015の白芯と同一パス4層グロー（色25%/45%/70%、白100%）にする。形状変調は±2%、スプライン相当の滑らかな閉ループとR×0.03の細いヒゲを追加。外側はR×2、1px白線、最大トゲ長R×0.10、閾値以下は点のみ。全周FFT割り当てと角度0°付近のクロスフェードを適用する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、形状と比率を維持したまま、内側グローを全alpha 1・lighter・同一パス5回へ置換する。HSLは1周約8秒、S=100%、L=55%。外側トゲは全周FFT割り当てで長さ=振幅×R×0.10、閾値以下は点のみ。書き出し前に彩度、40px位置の輝度、トゲ長比を自己検証し、条件未達なら出力しない。

今回の変更では、二重円リングを参照映像に合わせて作り直す。内側はなめらかなネオンの円（細い白芯＋約2.9秒周期のレインボーのブルーム）にし、時間波形のハイパス成分を巻き付けた房状の白い針を線上に走らせる。外側は1pxの白い16角形ローポリにし、全周の目盛りは弱い帯域を点、突出した帯域だけ長いトゲにする。FFTは対数軸で全周へ割り当て、12時位置の段差は掃引の折り返しへクロスフェードして消す。書き出し前チェックは帯域の強弱比ではなくピークの有無で判定する。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、二重円リングの半径を半分にし、帯域と角度の対応を約18秒で一周させて、キックのアタックが反応する位置を円周上で移動させる。内側リングのうねりはハーモニクスごとに向きと速さの違う回転を与えて常に組み変わるようにする。目盛りの太さと点の大きさは間隔に対する比で決め、径を変えても線が詰まらないようにする。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、音量への追従を入れる。フレームごとのピークで正規化するのをやめ、曲が大きいときの値を基準として保持し、現在の音との比でヒゲ・目盛り・うねり・グローの強さを決める。比には1より大きい指数をかけ、少し弱くなっただけで目に見えて弱くなるようにする。アナライザーのdB範囲を広げ、ラウドな音源で全帯域が上限に張り付かないようにする。外側リングは多角形をやめ、帯域をならしたなめらかな閉曲線にする。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、内側リングの色の帯を白芯よりはっきり太くし、時間で回る色相が読み取れるようにする。完了後に型チェック、本番ビルド、プレビューを確認する。

今回の変更では、横線スペクトラムにも二重円リングと同じ時間経過レインボーを適用する。色相の計算はヘルパーに切り出して両モードで共有する。完了後に型チェック、本番ビルド、プレビューを確認する。
